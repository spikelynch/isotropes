[project]
name = "isotropes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "botclient",
    "nltk>=3.9.1",
]

[tool.uv.sources]
botclient = { git = "https://github.com/spikelynch/botclient" }
